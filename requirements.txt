# --- Core Logic & YAML ---
# PyYAML is REQUIRED for 'import yaml' 
PyYAML>=6.0.1
# Required for the 'Healer' engine to preserve comments and formatting
ruamel.yaml>=0.17.21

# --- CLI Interface & Experience ---
# Required for the terminal colors and progress bars
rich==14.2.0
# Required for the command-line tab-completion (The missing piece!)
argcomplete>=3.0.0

# --- Compatibility & Utilities ---
typing-extensions>=4.0.0; python_version < "3.10"

# --- Testing & Quality (Dev Only) ---
# Needed for the automated "Inspector" we just set up
pytest>=8.0.0

# --- Build Tools ---
# Needed to turn your code into a single .exe or binary file
pyinstaller>=6.0.0
